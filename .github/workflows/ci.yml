name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-cov flake8

      - name: Lint
        run: flake8 src/ --count --select=E,W,F --max-line-length=120 --extend-ignore=W503,W504 --show-source --statistics

      - name: Test
        run: pytest tests/ -v --cov=src --cov-report=term-missing
